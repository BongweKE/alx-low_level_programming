#include "main.h"

/**
 * wildcmp - compare similarity of two strings
 * @s1: the first
 * @s2: second string
 *
 * Return: 1 if similar 0 if not
 */
int wildcmp(char *s1, char *s2)
{
	return (s1 == s2);
}
